<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="author" content="Charlie Bright">
  <meta name="description" content="A Theatre Company - Your go-to place for all things theatre.">

  <title>Theatre Company</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Custom CSS -->
  <link href="/css/style.css" rel="stylesheet">

  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
</head>

<body>
  <!-- Header -->
  <div class="container">
    <header class="d-flex flex-wrap justify-content-center py-3 border-bottom">
      
      <!-- Logo and Company Name -->
      <a href="/" class="d-flex align-items-center justify-content-center justify-content-lg-start mb-3 mb-lg-0 me-lg-auto link-body-emphasis text-decoration-none">
        <img id="header-logo" class="bi" src="/images/company/logo.webp" alt="Logo image for the company 'A Theatre Company'" width="110" height="50" aria-hidden="true">
        <span class="px-3 fs-4 fw-bold">A Theatre Co.</span>
      </a>
      
      
      <!-- Navigation Links -->
      <ul class="nav nav-pills justify-content-center text-center flex-wrap">

        <!-- Home -->
        <li class="nav-item">
          <a href="/" class="nav-link text-white d-flex align-items-center px-3 <%= currentPage === 'home' ? 'active' : '' %>">
            <i class="bi bi-house me-2"></i> Home
          </a>
        </li>

        <!-- Shows -->
        <li class="nav-item">
          <a href="/shows" class="nav-link text-white d-flex align-items-center px-3 <%= currentPage === 'shows' ? 'active' : '' %>">
            <i class="bi bi-calendar-event me-2"></i> Shows
          </a>
        </li>

        <!-- About -->
        <li class="nav-item">
          <a href="/about" class="nav-link text-white d-flex align-items-center px-3 <%= currentPage === 'about' ? 'active' : '' %>">
            <i class="bi bi-info-circle me-2"></i> About
          </a>
        </li>

        <!-- Contact -->
        <li class="nav-item">
          <a href="/contact" class="nav-link text-white d-flex align-items-center px-3 <%= currentPage === 'contact' ? 'active' : '' %>">
            <i class="bi bi-envelope me-2"></i> Contact
          </a>
        </li>
      </ul>

    </header>
  </div>
